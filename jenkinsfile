pipeline {
    agent any 
    stages{

        stage('Build'){
            steps{
                sh ''' 
                    docker-compose down --rmi all
                    docker-compose build
                    docker login -u suban92 -p Xconcept7
                    docker-compose push
                    '''
            }
        }
    }
}